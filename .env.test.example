# ===================================
# E2E Test Environment Variables
# ===================================
# This is a template file. Copy to .env.test and fill in your test database credentials.
#
# Setup Instructions:
# 1. Create a new Supabase project for testing (e.g., "ai-snippet-manager-test")
# 2. Copy this file: cp .env.test.example .env.test
# 3. Fill in the values below from your test Supabase project
# 4. Create test users in Supabase Dashboard → Authentication
# 5. Copy user UUIDs to the TEST_USER_ID fields below
#
# IMPORTANT: Never commit .env.test to git! It's already in .gitignore.
# ===================================

# ===================================
# Supabase Configuration
# ===================================
# Get these from: Supabase Dashboard → Project Settings → API
# Use your TEST Supabase project credentials here, not production
# These variable names match the main app requirements (PUBLIC_SUPABASE_*)

# Project URL (e.g., https://abcdefghijklmnop.supabase.co)
# Required by Astro app (src/lib/supabase.ts)
PUBLIC_SUPABASE_URL=https://YOUR_TEST_PROJECT_ID.supabase.co

# Anon/Public Key (safe to use in browser)
# Required by Astro app (src/lib/supabase.ts)
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_ANON_KEY_HERE

# Service Role Key (KEEP SECRET! Has admin privileges, bypasses RLS)
# Get from: Supabase Dashboard → Project Settings → API → service_role key
# Used by E2E test helpers for database cleanup operations
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_SERVICE_ROLE_KEY_HERE

# ===================================
# Test User #1 (Primary test user)
# ===================================
# Create in: Supabase Dashboard → Authentication → Users → Add user
# Email: e2e-test@example.com
# Password: TestPassword123!
# Auto-confirm user: YES
TEST_USER_EMAIL=e2e-test@example.com
TEST_USER_PASSWORD=TestPassword123!

# User UUID (copy from Supabase after creating user)
# Find in: Supabase Dashboard → Authentication → Users → (click user) → Copy UUID
TEST_USER_ID=00000000-0000-0000-0000-000000000000

# ===================================
# Test User #2 (For multi-user scenarios)
# ===================================
# Optional: Create second test user for testing user isolation
# Create same way as Test User #1
TEST_USER_2_EMAIL=e2e-test-2@example.com
TEST_USER_2_PASSWORD=TestPassword456!
TEST_USER_2_ID=00000000-0000-0000-0000-000000000001

# ===================================
# Application Settings
# ===================================
# Base URL for E2E tests (local dev server)
TEST_BASE_URL=http://localhost:4321

# ===================================
# Optional: External Services
# ===================================
# OpenAI API Key (optional: use mock responses or real key with low limits)
# For testing AI features - can use same key as dev or separate test key
# OPENAI_API_KEY=sk-test-your-openai-key-here
